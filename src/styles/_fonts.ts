import { Silkscreen, Courier_Prime as CourierPrime } from 'next/font/google';

export const silkscreen = Silkscreen({
  variable: '--font-silkcreen',
  weight: '400'
});

export const courierPrime = CourierPrime({
  variable: '--font-courier-prime',
  weight: ['400', '700']
});
